function makeDCPlot(dc, ds)


figure
plot(ds.descriptors.datesen, dc.semimaj, '--k');
datetick('x', 'mm-yy');
hold on
plot(ds.descriptors.datesen, smooth(dc.semimaj, 24*14), 'k', 'LineWidth', 2);
hold off
ylim([0 .5]);
xlim(ds.descriptors.datesen([1 end]));
grid on

set(gcf, 'FontSize', 16)
end